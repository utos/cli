<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <RootNamespace>Utos.Cli.Core</RootNamespace>
  </PropertyGroup>

  <!--
    Never reference Utos.Daemon.Server here: it defines the same utos.daemon.v1 service types as
    the client, and the two cannot coexist in one assembly.
  -->
  <ItemGroup>
    <PackageReference Include="Utos.Workflow" />
    <PackageReference Include="Utos.Workflow.Validation" />
    <PackageReference Include="Utos.Daemon.Client" />
    <PackageReference Include="Grpc.Net.Client" />
    <PackageReference Include="YamlDotNet" />
  </ItemGroup>

</Project>
